import React from "react";
import ReactDOM from "react-dom/client";

import EmbeddedGlobe from "./EmbeddedGlobe";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <EmbeddedGlobe />
  </React.StrictMode>,
);
