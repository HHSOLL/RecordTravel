name: core_ui
description: Shared design tokens and widgets for the Travel Atlas mobile app.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.8.0
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
