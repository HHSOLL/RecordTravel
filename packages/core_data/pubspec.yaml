name: core_data
description: Shared repositories, demo data, and app state providers for the Travel Atlas mobile app.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.8.0
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_riverpod: ^3.2.1
  drift: ^2.28.2
  core_domain:
    path: ../core_domain
  path: ^1.9.1
  path_provider: ^2.1.5

dev_dependencies:
  build_runner: ^2.6.0
  drift_dev: ^2.28.2
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
