name: core_domain
description: Shared product domain models for the Travel Atlas mobile app.
version: 0.1.0
publish_to: 'none'

environment:
  sdk: ^3.8.0
  flutter: '>=3.22.0'

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
